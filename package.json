{
  "name": "pqc-ethereum",
  "version": "1.0.0",
  "description": "Post-Quantum Cryptography anchored to Ethereum using CRYSTALS-Dilithium, Falcon, and Kyber",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}
